#!/usr/bin/env sh
set -e

# One ordered gate, the same list CI runs, so a green push means a green
# pipeline. `verify` is defined once in packages/utils/verify.mjs; Turbo caches
# unchanged tasks so re-runs are cheap. (E2E is not in verify — it runs as its
# own CI job, matching the source repos.)
pnpm run verify
